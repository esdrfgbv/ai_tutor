# Backend API URL (Vite inlines this at build time)
# Local dev — leave empty to fall back to http://localhost:8000
# Vercel production — must set in Vercel Dashboard: https://ai-tutor-sxt9.onrender.com
VITE_API_URL=
